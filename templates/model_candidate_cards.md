# 候选模型卡片

> 每张卡片只描述一个候选。分项判断必须带依据或 `unknown`；卡片不是最终选模结论。

## MOD-0001：模型标准名称

### 一句话用途

- 在本题中，它用 ______ 计算/预测 ______，主要处理 ______。
- 输入 → 输出：
- 为什么列入候选：

### 和基线的关系

- 基线先做什么：
- 候选新增什么能力：
- 候选额外付出什么：

### 分项评估

| 评估项 | 当前判断 | 对团队意味着什么 | 依据/未知 |
|---|---|---|---|
| 题目适配度 | 较匹配/有条件/不匹配/未知 |  |  |
| 假设负担 | 低/中/高/未知 |  |  |
| 数据准备度 | 已具备/需补充/不确定 |  |  |
| 可解释性 | 高/中/低/未知 |  |  |
| 计算与实现成本 | 低/中/高/未知 |  |  |
| 验证准备度 | 可验证/需设计/信息不足 |  |  |
| 潜在收益（待验证） |  |  |  |

### 关键假设与失败边界

- 关键假设：
- 可能失效的输入/约束/数据情形：
- 不应由当前说明推断的结论：

### 证据、未知和下一步

- 已有证据 `evidence_ids`：
- 仍未知：
- 最小验证 `validation_plan_ids`：
- 下一步验证优先级及理由：

### 理解校验

- 请用户用本题对象复述“输入 → 输出”：
- 请用户指出一个关键假设及其可能失败方式：
- 请用户指出相比基线增加的一项主要代价/风险：
- `understanding_check`：`not-asked` / `incomplete` / `answered` / `unknown`

### 人类确认

- 用户是否知道模型解决什么：`yes` / `no` / `unclear`
- 用户是否知道一个关键假设：`yes` / `no` / `unclear`
- 用户是否知道一个主要代价/风险：`yes` / `no` / `unclear`
- 用户选择：`adopt` / `reject` / `continue-validation` / `pause` / `undecided`
- `decision_id` / `decision_owner` / `decided_at`：
- `human_status`：`unreviewed` / `needs-human-confirmation` / `human-confirmed` / `rejected` / `unknown`
