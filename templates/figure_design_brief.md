# 图表设计简报

## 基本信息

- `figure_id`：
- `mode`：`brief` / `plot` / `redesign` / `assemble` / `diagram` / `handoff`
- `paper_location`：
- `target_reader`：
- `target_physical_size`：
- `source_version/hash`：

## 读者任务与证据

- 图表要回答的问题：
- 读者看完应完成的动作：
- 证据角色：
- 关联 `question_ids`：
- 关联 `claim_ids`：
- 关联 `evidence_ids` / `experiment_ids`：
- 正文承接位置和句子功能：

## 选图决策

- 数据关系：
- 候选图型：
- 最终图型及理由：
- 未采用的相邻图型及理由：
- 必须显示的基线/边界/不确定性：
- 不应由图形暗示的结论：

## 视觉系统

- 主焦点：
- 基线/候选/边界的视觉语义：
- 非颜色通道：
- 字体和公式设置：
- 轴、单位、图例和标注策略：
- 灰度/色觉/低分辨率检查：
- 多面板角色（如适用）：

## 生产与确认

- `source_data_ids`：
- `source_code_entry`：
- 转换步骤：
- 预计输出：
- 未解决项：
- 人类决定 ID：
- `human_status`：
