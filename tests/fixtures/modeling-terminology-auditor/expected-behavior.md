# 期望行为（非固定措辞测试）

- 正例：保留标准术语和有定义、可计算、跨产物复用的项目指标；全称/缩写/代码别名登记映射，不强行消除；
- 同义漂移：合并为一个 `concept_id`，选择规范名并逐处检查是否确有不同范围；
- 高级感包装：要求回退到“容量上限检查”“参数扰动实验”等直接表达，除非补充真实定义和证据；
- 符号/单位冲突：标为 P0/P1，不能全局替换或猜测哪个含义正确；
- 跨产物漂移：保留正文、数据字典、图表和代码证据，建立映射或请求人工确认；
- 任何修复都不得改变数字、公式、单位、指标含义、因果强度或作者决策。
