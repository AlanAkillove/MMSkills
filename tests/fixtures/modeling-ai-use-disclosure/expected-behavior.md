# Expected behavior

1. 草稿输入可生成带“草稿 / 待人工确认”标识的 PDF，并保留 unknown 和 partial 状态。
2. 未满足人工确认、记录完整和渲染状态条件时，`--final` 生成和 final 校验都应失败。
3. final 输入只有在每个事件有阶段、用途、采用、人工修改、核验、证据等级和 confirmed 状态时才通过。
4. PDF 只展示结构化摘要，不复制原始会话，不收录隐藏提示、隐藏思维过程、密钥或身份信息。
