# modeling-ai-use-disclosure fixtures

这些是虚构的、去身份化的结构化输入，不代表任何真实队伍的 AI 使用历史。

- `draft_input.json`：范围或工具元数据尚未完全确认，应生成草稿，不能生成 final。
- `final_input.json`：提供了示例性的人工确认记录，供脚本的 final 路径和 PDF 文本检查使用。

测试重点是：事件字段完整、采用/人工修改/核验分离、未知项显式化、提示/过程完整度不被伪造、PDF 生成必须经过人工确认门。
