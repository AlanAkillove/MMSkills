# Finding protocol fixtures

- 两个透镜观察的是同一底层问题，即使证据锚点不同，合并后也只保留一个 canonical finding；
- 两个来源 skill、两个 finding ID 和全部锚点必须可追溯；正文段落与图注上的同一问题应 union 锚点，而不是拆成两条；
- 合并脚本不得把不同严重性、冲突的 status/human_status 或冲突关系静默抹平，冲突应进入 `needs_human`。
