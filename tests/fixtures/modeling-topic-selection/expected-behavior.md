# 期望行为（非固定措辞测试）

- 先做全量题目盘点，每题有 `topic_id`、版本/快照、读取状态和卡片；缺失材料写 `unknown` 或 `blocked`。
- 用分项维度比较任务、背景、数据、验证、团队适配、时间和差异化空间；每项保留证据/未知及对团队的影响。
- 可以提供面向用户的通俗说明和主选/备选讨论，但不能用总分、默认推荐、获奖预测替代人工选题门。
- 只有明确的人类 `decision_id`、确认范围和反转条件后，才把选题交给题意、背景和文献阶段。
