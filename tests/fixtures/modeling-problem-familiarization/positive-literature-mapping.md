# 正例：文献思想与本题边界

来源 `SRC-02` 的 `literature_insight_id=LI-02` 说明其研究了某类流动结构，并记录 `source_does_not_support`：它没有证明本题数据或边界下的方法一定有效。

账本进一步写出可迁移的变量关系、不可迁移的尺度条件、与本题对象的相同/不同点和团队复述问题。后续只有在人工决定采用后，才把 `LI-02` 连接到 `model_decision_id`。
